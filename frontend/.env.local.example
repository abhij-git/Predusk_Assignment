# Optional. Leave unset for local dev: the UI calls /api/v1/... on the same host and Next.js
# proxies to FastAPI on 127.0.0.1:8000 (see next.config.mjs rewrites).
# NEXT_PUBLIC_API_URL=http://127.0.0.1:8000
